interface:
  display_name: "Inbox Forensic Action Operator"
  short_description: "Reconstruct inbox timelines and identify the real next action"
  default_prompt: "Use $inbox-forensic-action-operator-skill to audit the complete inbound and outbound history across the requested channels, reconstruct each entity timeline, apply strict offer and acceptance gates, identify the latest ball-in-court, and execute only the inbox or CRM actions explicitly authorized by the user."
