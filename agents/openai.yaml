interface:
  display_name: "Inbox & Recruiter Action Operator"
  short_description: "Reconstruct context and ship async-first recruiter packets"
  default_prompt: "Use $inbox-forensic-action-operator-skill to reconstruct the complete cross-channel opportunity state, prepare a tailored CV plus evidence brief when useful, minimize generic meetings, execute authorized actions once, and verify every result natively."
