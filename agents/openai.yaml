interface:
  display_name: "Inbox Forensic Action Operator"
  short_description: "Reconstruct cross-channel truth, then execute verified actions"
  default_prompt: "Use $inbox-forensic-action-operator-skill to recover prior context, build a complete source ledger across native Gmail, Close, meeting transcripts, every relevant GOWA device, calendars, ATS/provider pages, and authenticated browser forms; reconstruct each entity timeline, apply strict state gates, identify the newest ball-in-court, execute only the authorized drafts, sends, CRM updates, or form actions, preserve the live-send confirmation, and verify every mutation at the same external layer."
