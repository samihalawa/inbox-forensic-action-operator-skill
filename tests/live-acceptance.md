# Post-Install Live Acceptance

Run these read-only probes from the globally installed skill after structural fixtures pass. Unavailable sources remain named `CHECKPOINT` rows; they are not empty-source results.

1. **Frozen adjacent windows:** freeze one cutoff/timezone/identity set and replay two adjacent windows. Prove half-open non-overlap, later-window boundary ownership, stable merge keys, per-window coverage, and later-state override with original provenance retained.
2. **Email:** read the current connector result; use literal named-key direct transport when configured; read back the actual mailbox identity; execute one bounded cursorless search; prove Drafts/Sent separation and account-local versus requested-identity coverage.
3. **Authenticated browser:** on an existing signed-in surface record account, query, chronological sort, visible range/total, namespace, and page state. `Of many` must return partial.
4. **Close:** execute only the audit allowlist. The ledger must contain no new interactive login, reauthentication, OAuth/setup, ensure/create/default change, new browser profile, or CRM write. Record exact failure label, origin, and next proof when blocked.
5. **WhatsApp:** call current authenticated `/devices`; include every logged-in device; reconcile chat pages; compare raw and encoded JID on one read-only history; advance a short page by actual returned count; retain deficits; record media metadata/binary/semantic counts.
6. **Meeting:** read one invitation, cancellation, self-report, or provider occurrence. Keep scheduled, cancelled, user-reported completed, and provider-confirmed completed distinct.
7. **Provider alert:** bind one alert to live provider state when reachable. Alert mail alone must not promote outcome or remediation.
8. **Mutation guards:** verify audit creates nothing. In a controlled non-transmitting review, confirm draft remains draft, live send retains final preview/immediate confirmation, external submit/book/update retains its final confirmation affordance, and ambiguous results require target reread before retry.
9. **Output:** every source row includes coverage, failure origin, and proof remaining; every entity row includes latest event, proof layer, response owner, next-action owner, and action.

## Equality And Release Proof

1. Record source file paths and SHA-256 hashes for every runtime file.
2. Install the exact public repository/ref globally.
3. Re-run structural validation from source and installed copy.
4. Compare the installed runtime file set and hashes to source; no missing, extra, or differing runtime file passes.
5. Fetch the remote default branch and prove local `HEAD`, remote default ref, and the installed source commit/ref correspond.
6. Reconcile PRs, branches, worktrees, and stashes; remove only verified task-owned leftovers.
7. Record literal validator, live-probe, equality, and Git outputs in the release report.
